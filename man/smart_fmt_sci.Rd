% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt-hl.R
\name{smart_fmt_sci}
\alias{smart_fmt_sci}
\title{Format numbers using smart scientific notation}
\usage{
smart_fmt_sci(x, threshold = 1e-04, sci_digits = 2, num_digits = 4)
}
\arguments{
\item{x}{\code{numeric()} A numeric vector to format.}

\item{threshold}{\code{numeric(1)} Values with absolute value below this threshold
are formatted in scientific notation. Defaults to \code{0.0001}.}

\item{sci_digits}{\code{integer(1)} Number of significant digits for values
formatted in scientific notation. Defaults to \code{2}.}

\item{num_digits}{\code{integer(1)} Number of significant digits for values
formatted in fixed notation. Defaults to \code{4}.}
}
\value{
\code{character()} A character vector of formatted numbers.
}
\description{
Formats numeric values by applying scientific notation only when values fall
below a specified threshold, and fixed notation otherwise. This is useful for
variables with a wide range of small values where some require scientific
notation but others do not, such as p-values. The defaults are designed for
p-values.
}
\details{
Uses \code{\link[=formatC]{formatC()}} internally with \code{format = "e"} for scientific notation and
\code{format = "fg"} for fixed notation. The \code{"fg"} format uses significant digits
rather than decimal places, giving consistent precision across values of
different magnitudes.
}
\examples{
\dontrun{
x <- c(0.5, 0.0000001, 0.00032, 0.000000000123)

smart_fmt_sci(x)
#> [1] "0.5"      "1.00e-07" "0.00032"  "1.23e-10"

smart_fmt_sci(x, threshold = 0.001, sci_digits = 3, num_digits = 6)
#> [1] "0.5"       "1.000e-07" "3.200e-04" "1.230e-10"
}
}
\keyword{internal}
